[2026-06-05]
- Migrated 'Roles Can Set the Status' Select2 field (`js-example-basic-multiple`) to Tom Select (`tom_static_select`) in `#create_status_modal` and `#edit_status_modal` (`resources/views/modals.blade.php`).
- Fixed preselection of roles in the edit status modal by updating `custom.js` to correctly target `.tom_static_select` and use the Tom Select API (`setValue`) for assigning fetched roles.

- Unified Project Kanban board with the V2 UI design system (applied .tcard, .kcol logic from Taskify Revamp kit to projects-kanban-card.blade.php).
- Removed redundant Bootstrap .card wrapping around bootstrap-table components in tasks-card.blade.php and projects-card.blade.php to fix double-border UI spacing issues.


- Modernized the Projects Grid View (grid_view.blade.php) using the V2 Design System's .tcard component to achieve pixel-perfect consistency with the Kanban board.
- Re-styled inline select dropdowns on grid cards for compact layout alignment.


- Created a custom pagination component in resources/views/components/pagination.blade.php to match the V2 design system and applied it to the grid view, right-aligned.

- Upgraded Grid View filter dropdowns to use Tom Select and automatically apply filters upon selection, removing the manual filter button.

[2026-06-08]
- Updated `tk-table` component (`resources/views/components/tk-table.blade.php`) design by configuring standard Bootstrap 5 styling classes (`table table-hover align-middle`) in `$tableDefaults['data-classes']`.
- Fixed visual clipping on table headers by reverting `data-classes` override and increasing header vertical padding in `table.css`.
- Overhauled `table.css` for a premium SaaS aesthetic:
  - Added subtle `box-shadow: var(--shadow-1)` to `.bootstrap-table`.
  - Increased internal padding significantly across `.fixed-table-toolbar` (16px 20px), headers (16px 20px), cells (14px 20px), and `.fixed-table-pagination` (14px 20px) for a breathable design.
  - Transparentized table header backgrounds (`var(--bg-1)`) for modern cleanliness.
  - Added smooth hover background transitions to table rows.
- Redesigned Create Project and Update Project offcanvas (`resources/views/offcanvas.blade.php`) to adhere to V2 Design System:
  - Migrated `users_select`, `clients_select`, and `tags_select` from Select2 to Tom Select (`tom_users_select`, `tom_clients_select`, `tom_tags_select`).
  - Added `initTomSelectWithAjax(".tom_tags_select", "tags")` inside `public/assets/js/custom.js`.

[2026-06-14]
- Refactored `resources/views/leave_requests/list.blade.php`:
  - Added CSS grid-template-columns overrides for the Leave Balance card to render the 5 metrics evenly in one row on desktop and wrap gracefully on tablet/mobile views.
  - Wrapped the "Select Leave Editors" admin configuration form in a `.tk-card` component with proper design system headings (`.tk-card-head`, `.tk-card-title`, `.tk-card-title-sub`).
  - Converted the "Select Leave Editors" selector from `users_select` (Select2) to `tom_users_select` (Tom Select with AJAX) to align with standard design system inputs.
  - Fixed Tom Select dropdown clipping and z-index overlap by setting `overflow: visible; position: relative; z-index: 10;` on the parent `.tk-card`.
  - Updated Leave Requests table filter selects to use the correct initialization classes (`tom_users_select` and `tom_static_select`) so they render as Tom Select inputs.
  - Converted User and Visibility selectors inside Create Leave Request and Edit Leave Request modals (`resources/views/modals.blade.php`) to use Tom Select (`tom_users_select` class).
  - Aligned partial leave form inputs in Create and Edit modals by applying equal `20%` desktop widths to From Date, To Date, From Time, To Time, and Days fields, preventing layout wrapping.
  - Standardized Leave Balances page filters to use Tom Select:
    - Updated `#lb_status_filter` to `tom_static_select` and `#lb_member_filter` to `tom_users_select` in `leave_balances/index.blade.php`.
    - Configured `TableFilterSync` types to `tom-select` for both selectors in `public/assets/js/pages/leave-balances.js`.
    - Refactored cards, filters row, metric strips, charts card, and overview table wrappers in `leave_balances/index.blade.php` to completely align with V2 design system guidelines.
  - Refactored `paidLeaveWorkflowModal` styles (`public/assets/css/custom.css`) to use V2 Design Tokens (borders, backgrounds, shadows, text-colors, and callouts) ensuring consistent dark mode rendering and alignment with Brand styles.
  - Aligned and corrected the HTML grid row nesting in `create_leave_request_modal` (`resources/views/modals.blade.php`) to fix premature column wrapping and ensure fields flow properly inside a single row container.
  - Migrated select elements inside leave modals from Select2 to Tom Select by removing the conflicting `.users_select` class and updating event listener targets in `modals.blade.php` and `leave-requests.js`.
  - Refactored Activity Log page (`resources/views/activity_log/list.blade.php` and `public/assets/js/pages/activity-log.js`):
    - Migrated filters to Tom Select (`tom_users_select`, `tom_clients_select`, and `tom_static_select`) and updated `TableFilterSync` configuration types.
    - Updated clear filters button logic in `activity-log.js` to correctly invoke the Tom Select API.
    - Aligned filters layout inside a `.tk-filter-row` inside a top `card border shadow-none mb-3` filters card, and wrapped the table in a separate `card border shadow-none` card with a `.tk-table` wrapper to conform to V2 standards.
    - Migrated the Activity Log table from a raw `<table>` tag to the design system's `<x-tk-table>` blade component to inherit premium styling and standard pagination controls.
- Redesigned Social Media Scheduler Quick View Modal (`quickViewModal`):
  - Refactored `.qv-` styles in `public/assets/css/social/social.css` using theme CSS variables (`var(--bg-*)`, `var(--fg-*)`, `var(--line)`) for complete dark and light mode compatibility.
  - Upgraded status badges, platform cards, and statistics metrics to use correct semantic design tokens and semi-transparent colors.
  - Added action buttons (Publish Now, Edit, Delete, Close) inside the `modal-footer` of `quickViewModal` in `calendar.blade.php` and `index.blade.php` to restore missing UI functions.
  - Refactored `.day-post-item` in `social.css` with dynamic CSS variables to render correctly in dark mode.
